<ely:MyWindow x:Class="StickyHomeworks.Views.SettingsWindow"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
              xmlns:controls="clr-namespace:StickyHomeworks.Controls"
              xmlns:models="clr-namespace:StickyHomeworks.Models"
              xmlns:services="clr-namespace:StickyHomeworks.Services"
              xmlns:local="clr-namespace:StickyHomeworks2.Helpers"
              mc:Ignorable="d"
              d:DataContext="{d:DesignInstance views:SettingsWindow}"
              TextElement.Foreground="{DynamicResource MaterialDesignBody}"
              Background="{DynamicResource MaterialDesignPaper}"
              FontFamily="{StaticResource HarmonyOsSans}"
              TextElement.FontWeight="Regular"
              TextElement.FontSize="14"
              TextOptions.TextFormattingMode="Ideal"
              TextOptions.TextRenderingMode="Auto"
              Title="应用设置"
              Height="550"
              Width="1200"
              Icon="/Assets/AppLogo.png"
              MinWidth="600"
              Closing="SettingsWindow_OnClosing"
              FrameworkElement.FocusVisualStyle="{StaticResource {x:Static SystemParameters.FocusVisualStyleKey}}"
              xmlns:mdWpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
              xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
              xmlns:sys="clr-namespace:System;assembly=System.Runtime"
              xmlns:ely="http://github.com/HelloWRC/ElysiaFramework"
              xmlns:ci="clr-namespace:StickyHomeworks"
              xmlns:views="clr-namespace:StickyHomeworks.Views" 
              xmlns:markdig="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf">
    <Window.Resources>
        <Style TargetType="ListBoxItem"
                BasedOn="{StaticResource MaterialDesignListBoxItem}"
               x:Key="NotificationsListBoxItemStyle">
        </Style>
        <Style TargetType="{x:Type Hyperlink}">
            <Setter Property="local:HyperlinkBehavior.ConfirmNavigation"
                    Value="True"/>
        </Style>
        <ely:ColorToColorPickerBrushConverter x:Key="ColorToColorPickerBrushConverter"/>
        <Style TargetType="StackPanel"
                x:Key="SettingsPageStackPanelStyle">
            <Setter Property="Margin"
                    Value="12"/>
            <Setter Property="MaxWidth"
                    Value="750"/>
            <Style.Resources>
                <Style TargetType="ely:SettingsCard">
                    <Setter Property="Margin"
                            Value="0 0 0 6"/>
                </Style>
            </Style.Resources>
        </Style>
        <Style TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility"
                    Value="Auto"/>
            <Setter Property="materialDesign:ScrollViewerAssist.IsAutoHideEnabled"
                    Value="True"/>
            <Setter Property="PanningMode"
                    Value="Both"/>
        </Style>
        <ely:IntToStringConverter x:Key="IntToStringConverter"/>

        <Grid x:Key="OpenSourceLicense"
                Margin="16"
                Height="400"
                Width="550"
                MaxHeight="{Binding Height}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                    Text="开放源代码许可"
                    Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
            <TextBox Grid.Row="1"
                    Style="{StaticResource MaterialDesignOutlinedTextBox}"
                    Margin="0 12 0 0"
                     Padding="8"
                    Text="{Binding ViewModel.License, Mode=OneWay}"
                    IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                    FontFamily="Consolas"/>
            <Button Grid.Row="2"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Content="确定"
                    HorizontalAlignment="Right"
                    Margin="0 8 0 0"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}">

            </Button>
        </Grid>

        <Grid Margin="16"
                x:Key="ContributorsDrawer"
                Width="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                    Text="贡献人员"
                    Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
            <ListView Grid.Row="1"
                    Margin="0 6 0 0"
                      materialDesign:ListBoxItemAssist.ShowSelection="False">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:ContributionMember}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0"
                                    VerticalAlignment="Center"
                                   Margin="0 0 6 0"
                                    Source="{Binding AvatarImage}"
                                   Width="32"
                                    Height="32">
                                <Image.Clip>
                                    <RectangleGeometry RadiusX="16"
                                            RadiusY="16"
                                                       Rect="0,0,32,32"/>
                                </Image.Clip>
                            </Image>
                            <TextBlock Grid.Column="1"
                                    TextWrapping="Wrap">
                                <Run FontSize="15"
                                     Text="{Binding Name, Mode=OneWay}"/>
                                <LineBreak/>
                                <Run FontSize="11"
                                     FontWeight="Light"
                                     Text="{Binding Description, Mode=OneWay}"/>
                            </TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <models:ContributionMember
                    AvatarImage="/Assets/Contributiors/HelloWRC@64w.jpg"
                    Name="HelloWRC"
                        Description="软件作者，写代码巨水（划掉）"/>
                <models:ContributionMember
                    AvatarImage="/Assets/jizilin6732.jpg"
                    Name="晒~居"
                        Description="软件作者，Died（划掉）"/>
                <models:ContributionMember
                    AvatarImage="/Assets/ClassIsband.jpg"
                    Name="ClassIsband"
                        Description="什么都只会一点点的万能贡献者（划掉）"/>
            </ListView>
        </Grid>

        <Grid Margin="16"
                x:Key="ThirdPartyLibs"
                Width="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                    Text="第三方库"
                    Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
            <ListView Grid.Row="1"
                    Margin="0 6 0 0"
                    HorizontalContentAlignment="Stretch">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:ContributionLabel}">
                        <Grid>
                            <Grid.ToolTip>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="License"
                                                                 Margin="0 0 3 0"/>
                                        <TextBlock Text="{Binding License}"
                                                   Margin="0 0 12 0"
                                                TextWrapping="Wrap"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Web"
                                                                 Margin="0 0 3 0"/>
                                        <TextBlock Text="{Binding Website}"
                                                   Margin="0 0 12 0"
                                                TextWrapping="Wrap"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid.ToolTip>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <materialDesign:PackIcon Grid.Row="0"
                                    Grid.Column="0"
                                                     Kind="PackageVariant"
                                    VerticalAlignment="Center"
                                                     Margin="0 0 6 0"/>
                            <TextBlock Grid.Row="0"
                                    Grid.Column="1"
                                       Text="{Binding Title}"
                                       VerticalAlignment="Center"
                                    FontSize="14"
                                       FontWeight="Bold"
                                    TextWrapping="Wrap"/>
                            <!--<TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Description}"  Margin="0 3 0 0" TextWrapping="Wrap"/>-->
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <models:ContributionLabel Title="ElysiaFramework"
                                          License="GPLv3"
                                          Website="-"/>
                <models:ContributionLabel Title="MaterialDesignInXamlToolkit"
                                          License="MIT License"
                                          Website="https://github.com/MaterialDesignInXAML/MaterialDesignInXamlToolkit/"/>
                <models:ContributionLabel Title="gong-mdWpf-dragdrop"
                                          License="BSD 3-Clause"
                                          Website="https://github.com/punker76/gong-mdWpf-dragdrop"/>
                <models:ContributionLabel Title="H.NotifyIcon.Wpf"
                                          License="MIT License"
                                          Website="https://github.com/HavenDV/H.NotifyIcon"/>
                <models:ContributionLabel Title="MdXaml"
                                          License="MIT License"
                                          Website="https://github.com/whistyun/MdXaml"/>
                <models:ContributionLabel Title="Microsoft.AppCenter"
                                          License="MIT License"
                                          Website="https://aka.ms/telgml"/>
                <models:ContributionLabel Title="Downloader"
                                          License="MIT License"
                                          Website="https://github.com/bezzad/Downloader"/>
                <models:ContributionLabel Title="mdWpf-notifyicon"
                                          License="The Code Project Open License (CPOL) 1.02"
                                          Website="https://github.com/hardcodet/mdWpf-notifyicon"/>
                <models:ContributionLabel Title="HarmonyOS Sans"
                        License="-"
                                          Website="https://developer.harmonyos.com/cn/design/resource"/>
                <models:ContributionLabel Title="MVVM Toolkit"
                                          Website="https://github.com/CommunityToolkit/dotnet"
                                          License="MIT License"/>
                <models:ContributionLabel Title="WPF"
                        License="MIT License"
                                          Website="https://github.com/dotnet/mdWpf"/>
                <models:ContributionLabel Title=".NET"
                        License="MIT License"
                                          Website="https://github.com/microsoft/dotnet"/>
            </ListView>
        </Grid>

        <ScrollViewer 
                x:Key="ExperimentalSettings"
                      VerticalScrollBarVisibility="Auto"
                      materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True">
            <StackPanel Margin="16">
                <TextBlock Text="实验性功能"
                        Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                           Margin="0 0 0 4"/>
                <TextBlock Text="开发中的不稳定功能，可能会出现Bug等问题，请谨慎使用。"
                        TextWrapping="Wrap"
                           Margin="0 0 0 16"/>
                <MenuItem IsCheckable="True"
                        IsChecked="{Binding Settings.Debugginginterface}"
                           Header="设置 调试界面显隐 为 true"/>

            </StackPanel>
        </ScrollViewer>
        <ScrollViewer Width="332"
                x:Key="UpdateAdvancedSettings"
                      VerticalScrollBarVisibility="Auto"
                      materialDesign:ScrollViewerAssist.IsAutoHideEnabled="True">
            <StackPanel Margin="16">
                <TextBlock Text="高级更新设置"
                        Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                           Margin="0 0 0 4"/>
                <TextBlock Text="注意!如您不是专业人员,请谨慎设置。"
                        TextWrapping="Wrap"
                           Margin="0 0 0 16"/>



                <Button IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                        Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                        Click="CancelDownloadUpdateButton_Onclick"
                        Margin="0,10,0,0">
                    <ely:IconText Kind="DownloadOff"
                            Text="取消更新"
                            ToolTip="取消下载更新"/>
                </Button>
            </StackPanel>
        </ScrollViewer>

        <ely:BooleanToBooleanReConverter x:Key="BooleanToBooleanReConverter"/>
        <StackPanel x:Key="EditSubjectDialog"
                VerticalAlignment="Stretch"
                Margin="16"
                MaxWidth="400">
            <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                    Text="编辑科目名称"/>
            <TextBox Margin="0 6 0 0"
                    TextWrapping="Wrap"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding ViewModel.SubjectEditText}"
                     materialDesign:HintAssist.Hint="科目名称"/>
            <StackPanel Orientation="Horizontal"
                    Margin="0 8 0 0"
                    HorizontalAlignment="Right">
                <Button Content="确定"
                        HorizontalAlignment="Right"
                        Style="{DynamicResource MaterialDesignFlatButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="{Binding ViewModel.SubjectEditText, Mode=OneWay}"/>
                <Button Content="取消"
                        HorizontalAlignment="Right"
                        Style="{DynamicResource MaterialDesignFlatButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        Margin="6 0 0 0"
                        CommandParameter="{x:Null}"/>
            </StackPanel>
        </StackPanel>

        <StackPanel x:Key="EditTagDialog"
                VerticalAlignment="Stretch"
                Margin="16"
                MaxWidth="400">
            <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                    Text="编辑标签名称"/>
            <TextBox Margin="0 6 0 0"
                    TextWrapping="Wrap"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding ViewModel.TagEditText}"
                     materialDesign:HintAssist.Hint="标签名称"/>
            <StackPanel Orientation="Horizontal"
                    Margin="0 8 0 0"
                    HorizontalAlignment="Right">
                <Button Content="确定"
                        HorizontalAlignment="Right"
                        Style="{DynamicResource MaterialDesignFlatButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="{Binding ViewModel.TagEditText, Mode=OneWay}"/>
                <Button Content="取消"
                        HorizontalAlignment="Right"
                        Style="{DynamicResource MaterialDesignFlatButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        Margin="6 0 0 0"
                        CommandParameter="{x:Null}"/>
            </StackPanel>
        </StackPanel>
    </Window.Resources>

    <materialDesign:DialogHost Identifier="SettingsWindow">
        <materialDesign:DrawerHost RightDrawerContent="{Binding ViewModel.DrawerContent, Mode=OneWay}"
                                   DrawerClosing="MyDrawerHost_OnDrawerClosing"
                                   x:Name="MyDrawerHost">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <!-- 内容 -->
                <TabControl Grid.Column="2"
                        Grid.Row="1"
                        TabStripPlacement="Left"
                        x:Name="RootTabControl">
                    <!-- 基本 -->
                    <TabItem  Header="基本">
                        <ScrollViewer>
                            <StackPanel Style="{StaticResource SettingsPageStackPanelStyle}">
                                <TextBlock Text="基本"
                                    Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                                <TextBlock Margin="0 8 0 12" Opacity="0.75"
                                    Text="配置StickyHomeworks2一些基础性的功能"/>
                                <!-- 开机自启 -->
                                <ely:SettingsCard IconGlyph="AutomaticStart"
                                        Header="开机自启"
                                                  Description="在系统启动时自动运行本应用。"
                                                  IsOn="{Binding Settings.IsAutoStartEnabled, Mode=TwoWay}"/>
                                <!-- 自动清除作业 -->
                                <ely:SettingsExpanderCard IconGlyph="ClipboardTextClock"
                                                          Header="自动清除作业"
                                                          Description="当作业超出设定期限时，清除掉过期作业。"
                                                          IsOn="{Binding Settings.Autooutwork, Mode=TwoWay}"
                                                          ContentIconGlyph="TimerOutline"
                                                          ContentHeader="延迟清除"
                                                          ContentDescription="在首次展示过期作业后的第二天清除。"
                                                          Margin="0 0 0 6">
                                    <CheckBox IsChecked="{Binding Settings.DelayedCleanupEnabled, Mode=TwoWay}"
                                              IsEnabled="{Binding Settings.Autooutwork}"
                                              Content="启用"/>
                                </ely:SettingsExpanderCard>
                                <!-- 过期作业标记 -->
                                <ely:SettingsExpanderCard IconGlyph="Marker"
                                                          Header="过期作业标记"
                                                          Description="启用后，过期的作业内容将自动标记为指定颜色。"
                                                          IsOn="{Binding Settings.IsExpiredMarkEnabled, Mode=TwoWay}"
                                                          ContentIconGlyph="PaletteOutline"
                                                          ContentHeader="标记颜色"
                                                          ContentDescription="过期作业内容的显示颜色。"
                                                          Margin="0 0 0 6">
                                    <ely:ColorPicker Color="{Binding Settings.ExpiredMarkColor, Mode=TwoWay}"/>
                                </ely:SettingsExpanderCard>
                                <!-- 窗口标题 -->
                                <ely:SettingsCard IconGlyph="DesktopWindows"
                                        Header="窗口标题"
                                                  Description="在窗口上显示的标题。">
                                    <ely:SettingsCard.Switcher>
                                        <TextBox MinWidth="150"
                                                Text="{Binding Settings.Title}"/>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>



                                <!-- 最大面板宽度 -->
                                <ely:SettingsCard IconGlyph="DesktopWindows"
                                        Header="最大面板宽度"
                                                  Description="当作业超出显示范围换行时，各个列面板的最大宽度。">
                                    <ely:SettingsCard.Switcher>
                                        <ely:NumbericTextBox MinWidth="150"
                                                             Style="{StaticResource MaterialDesignTextBox}"
                                                             Text="{Binding Settings.MaxPanelWidth, Converter={StaticResource IntToStringConverter}}"
                                                             MinValue="0"
                                                             MaxValue="2000"
                                                             materialDesign:TextFieldAssist.SuffixText="px"/>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- 外观 -->
                    <TabItem Header="外观">
                        <ScrollViewer>
                            <StackPanel Style="{StaticResource SettingsPageStackPanelStyle}">
                                <TextBlock Text="外观"
                                    Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                                <TextBlock Margin="0 8 0 12" Opacity="0.75"
                                    Text="个性化StickyHomeworks2多样的外貌"/>
                                <ely:IconText Kind="CogOutline"
                                        Text="基本"
                                        Margin="0 0 0 8"/>
                                <!-- 界面缩放 -->
                                <ely:SettingsCard IconGlyph="ZoomInOutline"
                                        Header="界面缩放"
                                                  Description="主界面的缩放比例。">
                                    <ely:SettingsCard.Switcher>
                                        <Slider Width="180"
                                                Minimum="0.75"
                                                Maximum="5"
                                                VerticalAlignment="Center"
                                                Value="{Binding Settings.Scale}"
                                                AutoToolTipPlacement="BottomRight"
                                                TickFrequency="0.05"
                                                TickPlacement="None"
                                                IsSnapToTickEnabled="True"
                                                AutoToolTipPrecision="2"/>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>
                                <!-- 背景透明度 -->
                                <ely:SettingsCard IconGlyph="SquareTransparent"
                                        Header="背景不透明度"
                                                  Description="调节作业背景颜色的不透明度。">
                                    <ely:SettingsCard.Switcher>
                                        <Slider Width="180"
                                                Minimum="0"
                                                Maximum="1"
                                                Value="{Binding Settings.Opacity}"
                                                AutoToolTipPlacement="BottomRight"
                                                TickFrequency="0.01"
                                                IsSnapToTickEnabled="True"
                                                AutoToolTipPrecision="2"/>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>
                                <!-- 窗口置底 -->
                                <ely:SettingsCard IconGlyph="LayersOutline"
                                        Header="窗口置底"
                                                  Description="将窗口置于底端。"
                                                  IsOn="{Binding Settings.IsBottom, Mode=TwoWay}"/>
                                <!--标题颜色-->
                                    <ely:SettingsCard IconGlyph="PaletteOutline" Header="标题颜色"
                                                       Description="主界面标题颜色。">
                                    <ely:SettingsCard.Switcher>
                                        <ely:ColorPicker Color="{Binding Settings.TitleColor, Mode=TwoWay}" />
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>
                                <!--标题粗细-->
                                <ely:SettingsCard IconGlyph="FormatBold" Header="标题粗细"
                                                       Description="主界面标题粗细。">
                                    <ely:SettingsCard.Switcher>
                                        <ComboBox Foreground="{DynamicResource MaterialDesignBody}"
                                                  HorizontalContentAlignment="Left" SelectedValue="{Binding Settings.TitleFontWeight}">
                                            <FontWeight>Light</FontWeight>
                                            <FontWeight>Normal</FontWeight>
                                            <FontWeight>Medium</FontWeight>
                                            <FontWeight>Bold</FontWeight>
                                        </ComboBox>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>


                                <Separator Margin="0 16 0 8"/>
                                <ely:IconText Kind="PaletteOutline"
                                        Text="主题"
                                        Margin="0 0 0 8"/>
                                <!-- 应用主题 -->
                                <ely:SettingsCard IconGlyph="ThemeLightDark"
                                        Header="应用主题"
                                                  Description="控制应用的明暗主题。">
                                    <ely:SettingsCard.Switcher>
                                        <ComboBox Foreground="{DynamicResource MaterialDesignBody}"
                                                  SelectedIndex="{Binding Settings.Theme}"
                                                  HorizontalContentAlignment="Left">
                                            <ComboBoxItem HorizontalContentAlignment="Left">
                                                <ely:IconText Kind="ThemeLightDark"
                                                        Text="跟随系统"/>
                                            </ComboBoxItem>
                                            <ComboBoxItem HorizontalContentAlignment="Left">
                                                <ely:IconText Kind="WhiteBalanceSunny"
                                                        Text="明亮"/>
                                            </ComboBoxItem>
                                            <ComboBoxItem HorizontalContentAlignment="Left">
                                                <ely:IconText Kind="WeatherNight"
                                                        Text="黑暗"/>
                                            </ComboBoxItem>
                                        </ComboBox>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>
                                <!-- 主题色来源 -->
                                <ely:SettingsCard IconGlyph="PaletteSwatchOutline"
                                        Header="主题色来源"
                                                  Description="控制应用选取主题色的途径。">
                                    <ely:SettingsCard.Switcher>
                                        <ComboBox Foreground="{DynamicResource MaterialDesignBody}"
                                                  HorizontalContentAlignment="Left"
                                                  SelectedIndex="{Binding Settings.ColorSource}">
                                            <ComboBoxItem HorizontalContentAlignment="Left">
                                                <ely:IconText Kind="UserOutline"
                                                        Text="自定义"/>
                                            </ComboBoxItem>
                                            <ComboBoxItem HorizontalContentAlignment="Left">
                                                <ely:IconText Kind="Wallpaper"
                                                        Text="系统壁纸"/>
                                            </ComboBoxItem>
                                            <ComboBoxItem HorizontalContentAlignment="Left">
                                                <ely:IconText Kind="MicrosoftWindows"
                                                        Text="系统"/>
                                            </ComboBoxItem>
                                        </ComboBox>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>
                                <StackPanel>
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Settings.ColorSource}"
                                                        Value="0">
                                                    <Setter Property="Visibility"
                                                            Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                            <Setter Property="Visibility"
                                                    Value="Collapsed"/>
                                        </Style>
                                    </StackPanel.Style>
                                    <!-- 第一主题色 -->
                                    <ely:SettingsCard IconGlyph="PaletteOutline"
                                            Header="第一主题色"
                                                      Description="应用主要显示的主题颜色。">
                                        <ely:SettingsCard.Switcher>
                                            <ely:ColorPicker Color="{Binding Settings.PrimaryColor, Mode=TwoWay}"/>
                                        </ely:SettingsCard.Switcher>
                                    </ely:SettingsCard>
                                    <!-- 第二主题色 -->
                                    <ely:SettingsCard IconGlyph="PaletteOutline"
                                            Header="第二主题色"
                                                      Description="应用次要显示的主题颜色。">
                                        <ely:SettingsCard.Switcher>
                                            <ely:ColorPicker
                                                Color="{Binding Settings.SecondaryColor, Mode=TwoWay}"/>
                                        </ely:SettingsCard.Switcher>
                                    </ely:SettingsCard>
                                </StackPanel>
                                <!-- 壁纸主题色 -->
                                <StackPanel>
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Settings.ColorSource}"
                                                        Value="1">
                                                    <Setter Property="Visibility"
                                                            Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                            <Setter Property="Visibility"
                                                    Value="Collapsed"/>
                                        </Style>
                                    </StackPanel.Style>
                                    <Separator Margin="0 16 0 8"/>
                                    <ely:IconText Kind="Wallpaper"
                                            Text="壁纸主题色"
                                            Margin="0 0 0 8"/>
                                    <Grid>
                                        <Image Source="{Binding WallpaperPickingService.WallpaperImage, Mode=TwoWay}"
                                                Stretch="UniformToFill"/>
                                        <Grid VerticalAlignment="Bottom"
                                                IsEnabled="{Binding WallpaperPickingService.IsWorking, Mode=OneWay, Converter={StaticResource BooleanToBooleanReConverter}}">
                                            <Border Background="{DynamicResource MaterialDesignPaper}"
                                                    Opacity="0.85"/>
                                            <!-- 刷新 -->
                                            <Button Style="{StaticResource MaterialDesignFlatButton}"
                                                    Click="ButtonUpdateWallpaper_OnClick"
                                                    HorizontalAlignment="Left"
                                                    Margin="8 0">
                                                <ely:IconText Kind="Refresh"
                                                        Text="刷新"/>
                                            </Button>
                                            <!-- 颜色选择 -->
                                            <ListBox ItemsSource="{Binding Settings.WallpaperColorPlatte, Mode=OneWay}"
                                                     SelectedIndex="{Binding Settings.SelectedPlatteIndex}"
                                                     HorizontalAlignment="Right">
                                                <ListBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <WrapPanel IsItemsHost="True"/>
                                                    </ItemsPanelTemplate>
                                                </ListBox.ItemsPanel>
                                                <ListBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <materialDesign:ColorZone Background="{Binding Converter={StaticResource ColorToColorPickerBrushConverter}}"
                                                                                  Width="32"
                                                                Height="32"
                                                                                  Mode="Custom">
                                                            <materialDesign:PackIcon Kind="Check"
                                                                    VerticalAlignment="Bottom"
                                                                    HorizontalAlignment="Right"
                                                                                     Width="14"
                                                                    Height="14"
                                                                                     Background="White"
                                                                                     Foreground="Black"
                                                                                     Visibility="{Binding IsSelected, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListBoxItem}}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                                        </materialDesign:ColorZone>
                                                    </DataTemplate>
                                                </ListBox.ItemTemplate>
                                            </ListBox>
                                        </Grid>
                                        <!-- Loading -->
                                        <Grid HorizontalAlignment="Stretch"
                                                VerticalAlignment="Stretch"
                                              Visibility="{Binding WallpaperPickingService.IsWorking, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Border HorizontalAlignment="Stretch"
                                                    VerticalAlignment="Stretch"
                                                    Background="{DynamicResource MaterialDesignPaper}"
                                                    Opacity="0.8"/>
                                            <ely:StarRailLoadingControl/>
                                        </Grid>
                                    </Grid>
                                    <!-- 启用自动提取主题色 -->
                                    <ely:SettingsCard IconGlyph="RefreshAuto"
                                            Header="启用自动提取主题色"
                                            Margin="0 12 0 6"
                                                      Description="若启用，将会在每分钟刷新一次壁纸主题色，适用于会随时间大幅变化主题色的动态壁纸。"
                                                      IsOn="{Binding Settings.IsWallpaperAutoUpdateEnabled, Mode=TwoWay}"/>
                                    <!-- 自动提取主题色间隔 -->
                                    <ely:SettingsCard IconGlyph="ClockOutline"
                                            Header="自动提取主题色间隔"
                                                      Description="自动从壁纸提取主题色时间隔的时间，建议以动态壁纸更新速度按需设置，不宜低于10秒。">
                                        <ely:SettingsCard.Switcher>
                                            <ely:NumbericTextBox MaxValue="36000"
                                                    MinValue="0"
                                                    Width="150"
                                                                 materialDesign:TextFieldAssist.SuffixText="秒"
                                                                 Foreground="{DynamicResource MaterialDesignBody}"
                                                                 Style="{StaticResource MaterialDesignTextBox}"
                                                                 Text="{Binding Settings.WallpaperAutoUpdateIntervalSeconds, Mode=TwoWay, Converter={StaticResource IntToStringConverter}}"/>
                                        </ely:SettingsCard.Switcher>
                                    </ely:SettingsCard>
                                    <!-- 兼容模式 -->
                                    <ely:SettingsCard IconGlyph="CogOutline"
                                            Header="启用兼容模式"
                                                      Description="若启用，将从注册表提取壁纸，而非桌面句柄，不兼容动态壁纸。可能可以解决一些壁纸提取中的玄学问题。"
                                                      IsOn="{Binding Settings.IsFallbackModeEnabled, Mode=TwoWay}"/>
                                    <!-- 壁纸层窗口类名 -->
                                    <ely:SettingsCard IconGlyph="WindowMaximize"
                                            Header="壁纸层窗口类名"
                                                      Description="获取壁纸时要寻找的壁纸窗口的类名，留空将使用默认类名Progman。当需要从动态壁纸软件（如Wallpaper Engine等）获取壁纸时，请修改此选项为对应壁纸软件的壁纸窗口类名，以正确获取壁纸。若此栏未填写正确，将无法正常获取壁纸。">
                                        <ely:SettingsCard.Switcher>
                                            <DockPanel Width="150">
                                                <Button Style="{StaticResource MaterialDesignFlatButton}"
                                                        Content="{materialDesign:PackIcon WindowMaximize}"
                                                        ToolTip="浏览…"
                                                        DockPanel.Dock="Right"
                                                        Click="ButtonBrowseWindows_OnClick"/>
                                                <TextBox Foreground="{DynamicResource MaterialDesignBody}"
                                                         Text="{Binding Settings.WallpaperClassName}"/>
                                            </DockPanel>
                                        </ely:SettingsCard.Switcher>
                                    </ely:SettingsCard>
                                    <!-- 取色亮度波峰 -->
                                    <ely:SettingsCard IconGlyph="ChartBellCurve"
                                            Header="取色亮度波峰"
                                                      Description="取色时为亮度更接近此值的颜色赋予更高的权重，修改此值会影响到取色结果中整体颜色的亮度和准确性，请谨慎修改。（默认值：0.6）">
                                        <ely:SettingsCard.Switcher>
                                            <ely:NumbericTextBox MaxValue="1"
                                                    MinValue="0"
                                                    Width="150"
                                                                 Foreground="{DynamicResource MaterialDesignBody}"
                                                                 Style="{StaticResource MaterialDesignTextBox}"
                                                                 Text="{Binding Settings.TargetLightValue, Mode=TwoWay, Converter={StaticResource IntToStringConverter}}"/>
                                        </ely:SettingsCard.Switcher>
                                    </ely:SettingsCard>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- 科目 -->
                    <TabItem Header="科目">
                        <Grid Margin="12"
                                MaxWidth="750">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <DockPanel Grid.Row="0"
                                    Margin="0 0 0 8">
                                        <Button DockPanel.Dock="Right"
                                        Click="ButtonAddSubject_OnClick"
                                        Style="{StaticResource MaterialDesignFlatButton}">
                                            <ely:IconText Kind="Add"
                                            Text="添加科目"/>
                                        </Button>
                                        <TextBlock Text="科目"
                                        VerticalAlignment="Center"/>
                                    </DockPanel>
                                    <ListBox Grid.Row="1"
                                    ItemsSource="{Binding Settings.Subjects}"
                                     SelectionMode="Single"
                                     HorizontalContentAlignment="Stretch"
                                     SelectedIndex="{Binding ViewModel.SubjectSelectedIndex}">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{Binding}"
                                                    VerticalAlignment="Center"/>
                                                    <StackPanel Grid.Column="1"
                                                    Orientation="Horizontal"
                                                        Visibility="{Binding IsSelected, RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                        <Button Content="{materialDesign:PackIcon EditOutline}"
                                                        Style="{StaticResource MaterialDesignToolForegroundButton}"
                                                        Margin="4 -8"
                                                        ToolTip="编辑"
                                                        Click="ButtonEditSubject_OnClick"/>
                                                        <Button Content="{materialDesign:PackIcon DeleteOutline}"
                                                        Style="{StaticResource MaterialDesignToolForegroundButton}"
                                                        Margin="4 -8"
                                                        ToolTip="删除"
                                                        Foreground="IndianRed"
                                                        Click="ButtonDeleteSubject_OnClick"/>
                                                    </StackPanel>
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </Grid>
                    </TabItem>

                    <!-- 作业模板 -->
                    <TabItem Header="模板"
                             Loaded="TabHomeworkTemplate_OnLoaded">
                        <ScrollViewer>
                            <StackPanel Style="{StaticResource SettingsPageStackPanelStyle}">
                                <TextBlock Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                                           Text="作业模板"/>
                                <TextBlock Margin="0 8 0 12"
                                           Opacity="0.75"
                                           TextWrapping="Wrap"
                                           Text="配置作业编辑窗口中的快捷短语与作业条目"/>

                                <!-- 快捷操作 -->
                                <materialDesign:Card Margin="0 0 0 8"
                                                     materialDesign:ElevationAssist.Elevation="Dp1"
                                                     UniformCornerRadius="4">
                                    <StackPanel Margin="16">
                                        <StackPanel Orientation="Horizontal"
                                                    Margin="0 0 0 12">
                                            <materialDesign:PackIcon Kind="FlashOutline"
                                                                     VerticalAlignment="Center"
                                                                     Width="22"
                                                                     Height="22"
                                                                     Margin="0 0 10 0"
                                                                     Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="快捷短语"
                                                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>
                                                <TextBlock Margin="0 2 0 0"
                                                           Opacity="0.75"
                                                           TextWrapping="Wrap"
                                                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                           Text="在编辑窗口一键插入的短语，适合常用格式或固定前缀。"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <DockPanel Margin="0 -16 0 8">
                                            <Button DockPanel.Dock="Right"
                                                    Margin="8 0 0 0"
                                                    Style="{StaticResource MaterialDesignFlatButton}"
                                                    Click="ButtonHomeworkTemplateAddQuickAction_OnClick">
                                                <ely:IconText Kind="Plus"
                                                              Text="添加"/>
                                            </Button>
                                            <TextBox x:Name="TbHomeworkTemplateNewQuickAction"
                                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                                     materialDesign:HintAssist.Hint="新快捷短语"/>
                                        </DockPanel>
                                        <ListBox MinHeight="88"
                                                 MaxHeight="220"
                                                 materialDesign:ListBoxItemAssist.ShowSelection="False"
                                                 ItemsSource="{Binding Settings.HomeworkTemplate.QuickActions}"
                                                 HorizontalContentAlignment="Stretch">
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <DockPanel Margin="0 2">
                                                        <Button DockPanel.Dock="Right"
                                                                Style="{StaticResource MaterialDesignToolForegroundButton}"
                                                                ToolTip="删除"
                                                                Click="ButtonHomeworkTemplateRemoveQuickAction_OnClick">
                                                            <materialDesign:PackIcon Kind="DeleteOutline"/>
                                                        </Button>
                                                        <TextBlock Text="{Binding}"
                                                                   VerticalAlignment="Center"
                                                                   TextWrapping="Wrap"/>
                                                    </DockPanel>
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </StackPanel>
                                </materialDesign:Card>

                                <!-- 科目作业条目 -->
                                <materialDesign:Card Margin="0 0 0 6"
                                                     materialDesign:ElevationAssist.Elevation="Dp1"
                                                     UniformCornerRadius="4">
                                    <StackPanel Margin="16">
                                        <StackPanel Orientation="Horizontal"
                                                    Margin="0 0 0 12">
                                            <materialDesign:PackIcon Kind="BookEducationOutline"
                                                                     VerticalAlignment="Center"
                                                                     Width="22"
                                                                     Height="22"
                                                                     Margin="0 0 10 0"
                                                                     Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="科目作业条目"
                                                           Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>
                                                <TextBlock Margin="0 2 0 0"
                                                           Opacity="0.75"
                                                           TextWrapping="Wrap"
                                                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                           Text="按科目单独维护作业条目与子任务；「科目」页无条目时此处无可选科目。"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <ComboBox Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                                                  Margin="0 -12 0 12"
                                                  materialDesign:HintAssist.Hint="选择科目"
                                                  ItemsSource="{Binding Settings.Subjects}"
                                                  SelectedItem="{Binding ViewModel.HomeworkTemplateSelectedSubject, Mode=TwoWay}"
                                                  SelectionChanged="CbHomeworkTemplateSubject_OnSelectionChanged"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="1*"/>
                                                <ColumnDefinition Width="12"/>
                                                <ColumnDefinition Width="2*"/>
                                            </Grid.ColumnDefinitions>
                                            <DockPanel Grid.Column="0">
                                                <DockPanel DockPanel.Dock="Top"
                                                           Margin="0 0 0 6">
                                                    <TextBlock DockPanel.Dock="Top"
                                                        Margin="0 0 0 6"
                                                        Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                        Text="作业条目列表"/>
                                                    <Button DockPanel.Dock="Right"
                                                            Style="{StaticResource MaterialDesignFlatButton}"
                                                            Click="ButtonHomeworkTemplateAddSubjectBook_OnClick">
                                                        <ely:IconText Kind="Plus"
                                                                      Text="添加"/>
                                                    </Button>
                                                    <TextBox x:Name="TbHomeworkTemplateNewSubjectBook"
                                                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                                             materialDesign:HintAssist.Hint="本科作业条目名称"/>
                                                </DockPanel>
                                                
                                                <ListBox x:Name="LbHomeworkTemplateSubjectBooks"
                                                         MinHeight="128"
                                                         MaxHeight="240"
                                                         SelectionChanged="LbHomeworkTemplateSubjectBooks_OnSelectionChanged"
                                                         HorizontalContentAlignment="Stretch"/>
                                            </DockPanel>
                                            <StackPanel Grid.Column="2">
                                                <TextBlock Margin="0 0 0 6"
                                                           Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                           Text="需完成子任务"/>
                                                <DockPanel Margin="0 0 0 8">
                                                    <Button DockPanel.Dock="Right"
                                                            Style="{StaticResource MaterialDesignFlatButton}"
                                                            Click="ButtonHomeworkTemplateAddSubjectPart_OnClick">
                                                        <ely:IconText Kind="Plus"
                                                                      Text="添加"/>
                                                    </Button>
                                                    <TextBox x:Name="TbHomeworkTemplateNewSubjectPart"
                                                             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                                             materialDesign:HintAssist.Hint="子任务名称"/>
                                                </DockPanel>
                                                <ListBox x:Name="LbHomeworkTemplateSubjectParts"
                                                         MinHeight="128"
                                                         MaxHeight="240"
                                                         materialDesign:ListBoxItemAssist.ShowSelection="False"
                                                         HorizontalContentAlignment="Stretch">
                                                    <ListBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <DockPanel Margin="0 2">
                                                                <Button DockPanel.Dock="Right"
                                                                        Style="{StaticResource MaterialDesignToolForegroundButton}"
                                                                        ToolTip="删除"
                                                                        Click="ButtonHomeworkTemplateRemoveSubjectPart_OnClick">
                                                                    <materialDesign:PackIcon Kind="DeleteOutline"/>
                                                                </Button>
                                                                <TextBlock Text="{Binding}"
                                                                           VerticalAlignment="Center"
                                                                           TextWrapping="Wrap"/>
                                                            </DockPanel>
                                                        </DataTemplate>
                                                    </ListBox.ItemTemplate>
                                                </ListBox>
                                            </StackPanel>
                                        </Grid>
                                        <Button Margin="0 12 0 0"
                                                HorizontalAlignment="Left"
                                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                                Click="ButtonHomeworkTemplateDeleteSubjectBook_OnClick">
                                            <ely:IconText Kind="DeleteOutline"
                                                          Text="删除所选本科作业条目"/>
                                        </Button>
                                    </StackPanel>
                                </materialDesign:Card>

                                <!-- 通用作业条目 -->
                                <materialDesign:Card Margin="0 0 0 8"
                                    materialDesign:ElevationAssist.Elevation="Dp1"
                                    UniformCornerRadius="4">
                                    <StackPanel Margin="16">
                                        <StackPanel Orientation="Horizontal"
                                            Margin="0 0 0 12">
                                            <materialDesign:PackIcon Kind="BookMultipleOutline"
                                                 VerticalAlignment="Center"
                                                 Width="22"
                                                 Height="22"
                                                 Margin="0 0 10 0"
                                                 Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="通用作业条目"
                                                    Style="{StaticResource MaterialDesignSubtitle1TextBlock}"/>
                                                <TextBlock Margin="0 2 0 0"
                                                    Opacity="0.75"
                                                    TextWrapping="Wrap"
                                                    Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                    Text="所有科目可用的作业条目名称；每条目可配置多个「子任务」供勾选插入。"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <DockPanel Margin="0 -12 0 12">
                                            <Button DockPanel.Dock="Right"
                                                Margin="8 0 0 0"
                                                Style="{StaticResource MaterialDesignFlatButton}"
                                                Click="ButtonHomeworkTemplateAddCommonBook_OnClick">
                                                <ely:IconText Kind="Plus"
                                                       Text="添加作业条目"/>
                                            </Button>
                                            <TextBox x:Name="TbHomeworkTemplateNewCommonBook"
                                                Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                                materialDesign:HintAssist.Hint="作业条目名称"/>
                                        </DockPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="1*"/>
                                                <ColumnDefinition Width="12"/>
                                                <ColumnDefinition Width="2*"/>
                                            </Grid.ColumnDefinitions>
                                            <DockPanel Grid.Column="0">
                                                <TextBlock DockPanel.Dock="Top"
                                                    Margin="0 0 0 6"
                                                    Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                    Text="作业条目列表"/>
                                                <ListBox x:Name="LbHomeworkTemplateCommonBooks"
                                                    MinHeight="128"
                                                    MaxHeight="240"
                                                    SelectionChanged="LbHomeworkTemplateCommonBooks_OnSelectionChanged"
                                                    HorizontalContentAlignment="Stretch"/>
                                            </DockPanel>
                                            <StackPanel Grid.Column="2">
                                                <TextBlock Margin="0 0 0 6"
                                                    Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                    Text="需完成子任务"/>
                                                <DockPanel Margin="0 -10 0 8">
                                                    <Button DockPanel.Dock="Right"
                                                        Margin="8 0 0 0"
                                                        Style="{StaticResource MaterialDesignFlatButton}"
                                                        Click="ButtonHomeworkTemplateAddCommonPart_OnClick">
                                                        <ely:IconText Kind="Plus"
                                                               Text="添加"/>
                                                    </Button>
                                                    <TextBox x:Name="TbHomeworkTemplateNewCommonPart"
                                                        Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                                                        materialDesign:HintAssist.Hint="子任务名称"/>
                                                </DockPanel>
                                                <ListBox x:Name="LbHomeworkTemplateCommonParts"
                                                    MinHeight="128"
                                                     MaxHeight="240"
                                                     materialDesign:ListBoxItemAssist.ShowSelection="False"
                                                     HorizontalContentAlignment="Stretch">
                                                    <ListBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <DockPanel Margin="0 2">
                                                                <Button DockPanel.Dock="Right"
                                                                    Style="{StaticResource MaterialDesignToolForegroundButton}"
                                                                     ToolTip="删除"
                                                                     Click="ButtonHomeworkTemplateRemoveCommonPart_OnClick">
                                                                    <materialDesign:PackIcon Kind="DeleteOutline"/>
                                                                </Button>
                                                                <TextBlock Text="{Binding}"
                                                                    VerticalAlignment="Center"
                                                                    TextWrapping="Wrap"/>
                                                            </DockPanel>
                                                        </DataTemplate>
                                                    </ListBox.ItemTemplate>
                                                </ListBox>
                                            </StackPanel>
                                        </Grid>
                                        <Button Margin="0 12 0 0"
                                            HorizontalAlignment="Left"
                                             Style="{StaticResource MaterialDesignOutlinedButton}"
                                             Click="ButtonHomeworkTemplateDeleteCommonBook_OnClick">
                                            <ely:IconText Kind="DeleteOutline"
                                                 Text="删除所选通用作业条目"/>
                                        </Button>
                                    </StackPanel>
                                </materialDesign:Card>
                                
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- 标签 -->
                    <TabItem Header="标签">
                        <Grid Margin="12"
                                MaxWidth="750">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <DockPanel Grid.Row="0"
                                    Margin="0 0 0 8">
                                <Button DockPanel.Dock="Right"
                                        Click="ButtonAddTag_OnClick"
                                        Style="{StaticResource MaterialDesignFlatButton}">
                                    <ely:IconText Kind="Add"
                                            Text="添加标签"/>
                                </Button>
                                <TextBlock Text="标签"
                                        VerticalAlignment="Center"/>
                            </DockPanel>
                            <ListBox Grid.Row="1"
                                    ItemsSource="{Binding Settings.Tags}"
                                     SelectionMode="Single"
                                     HorizontalContentAlignment="Stretch"
                                     SelectedIndex="{Binding ViewModel.TagSelectedIndex}"
                                     Style="{StaticResource MaterialDesignChoiceChipOutlineListBox}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding}"
                                                    VerticalAlignment="Center"/>
                                            <StackPanel Grid.Column="1"
                                                    Orientation="Horizontal"
                                                    VerticalAlignment="Center"
                                                    Margin="4 0 0 0"
                                                        Visibility="{Binding IsSelected, RelativeSource={RelativeSource FindAncestor, AncestorType=ListBoxItem}, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <Button Content="{materialDesign:PackIcon EditOutline, Size=16}"
                                                        Style="{StaticResource MaterialDesignToolForegroundButton}"
                                                        Margin="0 -8"
                                                        ToolTip="编辑"
                                                        Click="ButtonEditTag_OnClick"/>
                                                <Button Content="{materialDesign:PackIcon DeleteOutline, Size=16}"
                                                        Style="{StaticResource MaterialDesignToolForegroundButton}"
                                                        Margin="0 -8"
                                                        ToolTip="删除"
                                                        Foreground="IndianRed"
                                                        Click="ButtonDeleteTag_OnClick"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </TabItem>


                    <TabItem Header="更新">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <ScrollViewer Grid.Row="0">
                                <StackPanel Margin="6"
                                        MaxWidth="750">
                                    <Grid HorizontalAlignment="Stretch"
                                            Margin="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>
                                        <materialDesign:Ripple Grid.Column="0"
                                                VerticalAlignment="Top"
                                                               MouseDown="AppIcon_OnMouseDown">
                                            <materialDesign:PackIcon x:Name="UpdateIcon"
                                                    Kind="Update"
                                                    Width="50"
                                                    Height="50"
                                                    VerticalAlignment="Top"/>
                                        </materialDesign:Ripple>
                                        <StackPanel Grid.Column="1"
                                                Margin="5 0 0 0">
                                            <TextBlock Height="46">
                                                <Run Text="未获取更新"
                                                        x:Name="UpdateStatusTextBlock"
                                                        FontSize="35"
                                                        FontWeight="Medium"/>
                                                    <LineBreak/>
                                            </TextBlock>
                                            <ProgressBar x:Name="DownloadProgress"
                                                    Height="4"
                                                    Margin="0,10,0,0"
                                                    Visibility="Collapsed"
                                                    Minimum="0"
                                                    Maximum="100"
                                                    Value="0"
                                                         Style="{StaticResource MaterialDesignLinearProgressBar}"/>
                                            <TextBlock x:Name="versionStatusTextBlock"
                                                    FontSize="15"
                                                    Margin="0,10,0,0"
                                                    Text="{Binding Source={x:Static ci:App.AppVersion},  StringFormat='当前版本：{0}'}"/>

                                            <Grid HorizontalAlignment="Stretch">
                                                <StackPanel  Orientation="Horizontal"
                                                        Margin="-12 0">
                                                    <Button x:Name="CheckUpdatesButton"
                                                            HorizontalAlignment="Left"
                                                            Margin="10"
                                                            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                                                            Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                                                            Click="CheckUpdatesButton_Onclick">
                                                        <ely:IconText Kind="restart"
                                                                Text="检查更新"
                                                                ToolTip="检查更新"/>
                                                    </Button>
                                                    <Button x:Name="DownloadUpdatesButton"
                                                            HorizontalAlignment="Left"
                                                            Margin="10"
                                                            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                                                            Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                                                            Click="DownloadUpdatesButton_Onclick"
                                                            Visibility="Collapsed">
                                                        <ely:IconText Kind="Download"
                                                                Text="下载更新"
                                                                ToolTip="立即下载更新"/>
                                                    </Button>
                                                    <Button x:Name="InstallUpdatesButton"
                                                            HorizontalAlignment="Left"
                                                            Margin="10"
                                                            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                                                            Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                                                            Click="InstallUpdatesButton_Onclick"
                                                            Visibility="Collapsed">
                                                        <ely:IconText Kind="Autorenew"
                                                                Text="立即重启"
                                                                ToolTip="立即安装更新"/>
                                                    </Button>
                                                    <Button x:Name="CancelInstallUpdatesButton"
                                                            HorizontalAlignment="Left"
                                                            Margin="10"
                                                            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                                                            Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                                                            Click="CancelInstallUpdatesButton_Onclick"
                                                            Visibility="Collapsed">
                                                        <ely:IconText Kind="CloseThick"
                                                                Text="取消安装"
                                                                ToolTip="取消安装更新"/>
                                                    </Button>
                                                    <Button x:Name="CancelDownloadUpdateButton"
                                                            HorizontalAlignment="Left"
                                                            Margin="10"
                                                            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                                                            Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                                                            Click="CancelDownloadUpdateButton_Onclick"
                                                            Visibility="Collapsed">
                                                        <ely:IconText Kind="DownloadOff"
                                                                Text="取消更新"
                                                                ToolTip="取消下载更新"/>
                                                    </Button>
                                                    <Button Content="{materialDesign:PackIcon DotsHorizontal}"
                                                            IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                                                            Style="{StaticResource MaterialDesignRaisedSecondaryButton}"
                                                            ToolTip="更多设置"
                                                            Click="UpdateAdvancedSettingsButton_Onclick"/>
                                                </StackPanel>
                                            </Grid>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </ScrollViewer>

                            <TabControl x:Name="tabControl" Margin="100,0,100,0" Grid.Row="1" >
                                <TabItem Header="更新日志">
                                    <FlowDocumentScrollViewer x:Name="DocumentViewer"
                                              Foreground="{DynamicResource MaterialDesignBody}"
                                              Background="{DynamicResource MaterialDesignPaper}"
                                              BorderThickness="0"
                                              IsSelectionEnabled="True"
                                              VerticalScrollBarVisibility="Auto"
                                              PreviewMouseWheel="DocumentViewer_PreviewMouseWheel"/>
                                </TabItem>

                                <TabItem Header="更新设置">
                                    <StackPanel Style="{StaticResource SettingsPageStackPanelStyle}" Margin="0">
                                        <materialDesign:Card Margin="12">
                                            <StackPanel Margin="12">
                                                <ely:SettingsControl IconGlyph="DownloadOutline"
                                                                     Header="更新通道"
                                                                     Description="控制应用的更新目标版本。">
                                                    <ely:SettingsControl.Switcher>
                                                        <Grid>
                                                            <ComboBox Width="150"
                                                                  Foreground="{DynamicResource MaterialDesignBody}"
                                                                  SelectedIndex="{Binding Settings.UpdateChannel}"
                                                                  SelectionChanged="ComboBoxUpdateChannel_OnSelectionChanged">
                                                            <ComboBoxItem Content="稳定版" HorizontalContentAlignment="Left"/>
                                                            <ComboBoxItem Content="测试版" HorizontalContentAlignment="Left"/>
                                                        </ComboBox>
                                                        </Grid>
                                                    </ely:SettingsControl.Switcher>
                                                </ely:SettingsControl>
                                                <TextBlock x:Name="UpdateChannelDescriptionTextBlock"
                                                           Margin="0 8 0 0"
                                                           FontWeight="Regular"
                                                           TextWrapping="Wrap"
                                                           Text="接收经过充分测试的稳定版本更新。" />
                                            </StackPanel>
                                        </materialDesign:Card>
                                    </StackPanel>
                                </TabItem>
                            </TabControl>
                        </Grid>
                    </TabItem>

                    <!-- ClassIsland -->
                    <TabItem Header="CI联动">
                        <ScrollViewer>
                            <StackPanel Style="{StaticResource SettingsPageStackPanelStyle}">
                                <TextBlock Text="ClassIsland 联动"
                                    Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                                <TextBlock Margin="0 8 0 12" Opacity="0.75"
                                    Text="通过 ClassIsland IPC 服务，根据课程状态自动控制主界面的显示与隐藏"/>

                                <ely:SettingsCard IconGlyph="LanConnect"
                                    Header="启用 ClassIsland 联动"
                                    Description="连接 ClassIsland IPC 服务，监听课程状态变化"
                                    IsOn="{Binding Settings.IsClassIslandIpcEnabled, Mode=TwoWay}"/>

                                <controls:ClassIslandSubjectsCard 
                                    Subjects="{Binding Settings.ClassIslandSubjects}"
                                    RefreshClicked="ButtonRefreshClassIslandSubjects_OnClick"
                                    ImportClicked="ButtonImportClassIslandSubjects_OnClick"
                                    Margin="0 0 0 6"/>

                                <ely:SettingsCard IconGlyph="Connection"
                                    Header="连接状态"
                                    Description="ICP服务活动状态">
                                    <ely:SettingsCard.Switcher>
                                        <TextBlock Text="{Binding ClassIslandIpcService.ConnectionStatus}"
                                            Opacity="0.75" VerticalAlignment="Center"/>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>

                                
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>


                    <!-- 关于 -->
                    <TabItem Header="KnotLink">
                        <ScrollViewer>
                            <StackPanel Style="{StaticResource SettingsPageStackPanelStyle}">
                                <TextBlock Text="KnotLink 联动"
                                    Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                                <TextBlock Margin="0 8 0 12" Opacity="0.75"
                                    Text="通过 KnotLink 协议接收教师端远程请求，实现作业数据同步"/>

                                <ely:SettingsCard IconGlyph="Connection"
                                    Header="连接状态"
                                    Description="KnotLink 服务当前运行状态">
                                    <ely:SettingsCard.Switcher>
                                        <TextBlock Text="{Binding KnotLinkService.StatusText}"
                                            Opacity="0.75" VerticalAlignment="Center"/>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>

                                <ely:SettingsCard IconGlyph="Identifier"
                                    Header="App ID"
                                    Description="KnotLink 应用标识符">
                                    <ely:SettingsCard.Switcher>
                                        <TextBlock Text="{Binding KnotLinkService.AppId}"
                                            Opacity="0.75" VerticalAlignment="Center"/>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>

                                <ely:SettingsCard IconGlyph="AccessPoint"
                                    Header="OpenSocket ID"
                                    Description="KnotLink 开放套接字标识符">
                                    <ely:SettingsCard.Switcher>
                                        <TextBlock Text="{Binding KnotLinkService.OpenSocketId}"
                                            Opacity="0.75" VerticalAlignment="Center"/>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <TabItem Header="关于">
                        <ScrollViewer>
                            <StackPanel Margin="6"
                                    MaxWidth="750">
                                <TextBlock Margin="0 6" Text="关于"
                                    Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                                <TextBlock Margin="0 2 0 12" Opacity="0.75"
                                    Text="关于StickyHomeworks2"/>
                                <materialDesign:Card ClipContent="False">
                                    <StackPanel>
                                        <Image Source="/Assets/banner.png"

/>
                                        <Grid HorizontalAlignment="Stretch"
                                                Margin="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition/>
                                            </Grid.ColumnDefinitions>
                                            <!-- icon -->
                                            <materialDesign:Ripple Grid.Column="0"
                                                    VerticalAlignment="Top"
                                                    MouseDown="AppIcon_OnMouseDown">
                                                <Image Source="/Assets/AppLogo.ico"
                                                        Width="70"
                                                        VerticalAlignment="Top"/>
                                            </materialDesign:Ripple>
                                            <!-- about -->
                                            <StackPanel Grid.Column="1"
                                                    Margin="10 0 0 0">
                                                <Grid>
                                                    <TextBlock>
                                                        <Run Text="StickyHomework2"
                                                                FontSize="24"
                                                                FontWeight="Bold"/>
                                                        <LineBreak/>
                                                        <Run Text="{x:Static ci:App.AppVersion}"/>
                                                        <LineBreak/>
                                                        <Run Text="Copyright (c) 2023 HelloWRC"/>
                                                    </TextBlock>
                                                    <WrapPanel HorizontalAlignment="Right"
                                                            VerticalAlignment="Bottom"
                                                            Margin="2 2">
                                                        <Button Content="{materialDesign:PackIcon Github}"
                                                                IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                                                                ToolTip="Github"
                                                                Click="ButtonGithub_Click"
                                                                Margin="2 2">
                                                        </Button>
                                                        <Button Content="{materialDesign:PackIcon BookOpenVariant}"
                                                                IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                                                                ToolTip="文档"
                                                                Click="ButtonDocs_Click"
                                                                Margin="2 2">
                                                        </Button>
                                                        <Button Content="{materialDesign:PackIcon Dropbox}"
                                                                IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                                                                ToolTip="第三方库"
                                                                Click="ButtonThirdPartyLibs_OnClick"
                                                                Margin="2 2">
                                                        </Button>
                                                        <Button Content="{materialDesign:PackIcon AccountMultiple}"
                                                                IsEnabled="{Binding DataContext.ControlsEnabled, RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                                                                ToolTip="贡献者"
                                                                Click="ButtonContributors_OnClick"
                                                                Margin="2 2">
                                                        </Button>

                                                    </WrapPanel>
                                                </Grid>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </materialDesign:Card>
                                <materialDesign:Card Margin="0 12 0 0" ClipContent="True"
                                                     Background="{DynamicResource MaterialDesignCardBackground}"
                                                     Cursor="Hand"
                                                     MouseLeftButtonDown="EchoCave_OnMouseLeftButtonDown">
                                    <StackPanel>
                                        <TextBlock Text="回声洞" FontWeight="Bold" Margin="16 12 16 6"/>
                                        <controls:TypingControl Margin="24 8 16 12"
                                                                 Text="{Binding ViewModel.EchoText, Mode=OneWay}"
                                                                 IsBusy="{Binding ViewModel.IsEchoBusy, Mode=OneWayToSource}"/>
                                    </StackPanel>
                                </materialDesign:Card>
                                <FlowDocumentScrollViewer ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                                          IsSelectionEnabled="False"
                                                          PreviewMouseWheel="UIElement_OnPreviewMouseWheel"
                                                          ContextMenu="{x:Null}">
                                    <FlowDocument FontFamily="{StaticResource HarmonyOsSans}">
                                        <Paragraph>
                                            目前小部分功能尚不完善，可能会出现bug，会在后期的版本中完善或修复。
                                        </Paragraph>
                                        <Paragraph>
                                            饼要一个一个画，功能要一个一个实现（
                                        </Paragraph>
                                        <Paragraph>
                                            Work In Progress
                                        </Paragraph>
                                        <Paragraph Foreground="#fff7d1ec">
                                            永远喜欢爱莉希雅
                                        </Paragraph>
                                    </FlowDocument>
                                </FlowDocumentScrollViewer>
                                <FlowDocumentScrollViewer ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                        IsSelectionEnabled="False"
                                        PreviewMouseWheel="UIElement_OnPreviewMouseWheel"
                                        ContextMenu="{x:Null}">
                                    <FlowDocument FontFamily="{StaticResource HarmonyOsSans}">
                                        <Paragraph Style="{StaticResource MarkdownHeadline1Style}">
                                            鸣谢
                                        </Paragraph>
                                        <Paragraph>
                                            <Run Text="由 " />
                                            <Hyperlink NavigateUri="https://www.yesongit.com/">苏州晔淞信息科技有限公司</Hyperlink>
                                            <Run Text=" 提供云计算支持" />
                                        </Paragraph>
                                    </FlowDocument>
                                </FlowDocumentScrollViewer>

                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>

                    <!-- 调试 -->
                    <TabItem Header="调试" x:Name="Debugginginterface">
                        <ScrollViewer>
                            <StackPanel Style="{StaticResource SettingsPageStackPanelStyle}">
                                <StackPanel.Resources>
                                    <Style TargetType="MenuItem"
                                            BasedOn="{StaticResource MaterialDesignMenuItem}">
                                        <Style.Resources>
                                            <Style TargetType="materialDesign:PackIcon">
                                                <Setter Property="Foreground"
                                                        Value="{DynamicResource PrimaryHueMidBrush}"/>
                                            </Style>
                                        </Style.Resources>
                                    </Style>
                                </StackPanel.Resources>
                                <TextBlock Text="调试"
                                        Style="{StaticResource MaterialDesignHeadline5TextBlock}"/>
                                <TextBlock Margin="0 12">
                                    <Run Text="仅供调试，除非你能知道您在做什么，请不要使用以下按钮。"></Run>
                                </TextBlock>
                                <ely:SettingsCard IconGlyph="School"
                                    Header="当前科目"
                                    Description="ClassIsland 当前识别的科目">
                                    <ely:SettingsCard.Switcher>
                                        <TextBlock Text="{Binding ClassIslandIpcService.CurrentSubjectName}"
                                            Opacity="0.75" VerticalAlignment="Center"/>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>

                                <ely:SettingsCard IconGlyph="Clock"
                                    Header="当前时间状态"
                                    Description="ClassIsland 当前时间状态">
                                    <ely:SettingsCard.Switcher>
                                        <TextBlock Text="{Binding ClassIslandIpcService.CurrentState}"
            Opacity="0.75" VerticalAlignment="Center"/>
                                    </ely:SettingsCard.Switcher>
                                </ely:SettingsCard>
                                <MenuItem Icon="{materialDesign:PackIcon BugOutline}"
                                        Header="崩溃测试"
                                        Click="ButtonCrash_OnClick"/>
                                <MenuItem Icon="{materialDesign:PackIcon CloseBoxMultiple}"
                                        Header="多开警告"
                                        Click="MultipleOpeningsText_OnClick"/>
                                <MenuItem IsCheckable="True"
                                        IsChecked="{Binding Settings.IsDebugShowInTaskBar}"
                                        Header="在任务栏显示图标"/>
                                <MenuItem Icon="{materialDesign:PackIcon LightningBoltOutline}"
                                        Header="测试作业编辑窗口"
                                        Click="MenuItemTestHomeworkEditWindow_OnClick"/>
                                <MenuItem Icon="{materialDesign:PackIcon LanConnect}"
                                        Header="测试IPC联动"
                                        Click="ButtonTestIpcEvent_OnClick"/>
                                <MenuItem Icon="{materialDesign:PackIcon TextBoxOutline}"
                                        Header="查看日志…"
                                        Click="ButtonViewLogs_OnClick"/>

                                <RichTextBox>
                                    <FlowDocument IsHyphenationEnabled="False"
                                                  IsOptimalParagraphEnabled="True">

                                    </FlowDocument>
                                </RichTextBox>
                                <TextBox Text="{Binding ViewModel.DebugRichTextBoxContent, Mode=TwoWay}"/>
                                <TextBlock Margin="0 12">
                                    <Hyperlink NavigateUri="https://www.kimi.com/chat/d3h9noh3ntnibd5skui0">测试跳转链接提醒</Hyperlink>
                                </TextBlock>
                            </StackPanel>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
                <materialDesign:ColorZone Grid.Column="0"
                        Grid.ColumnSpan="3"
                        Grid.Row="0"
                        Padding="16"
                        Mode="PrimaryMid"
                                          materialDesign:ElevationAssist.Elevation="Dp4"
                                          Margin="0 0 0 4">
                    <Grid Margin="4 0 4 0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <!--<ToggleButton Grid.Column="0"
                            Style="{StaticResource MaterialDesignHamburgerToggleButton}" />-->
                        <!--<Button ToolTip="导航栏"
                                VerticalAlignment="Center"
                                Padding="0"
                                Style="{StaticResource MaterialDesignToolForegroundButton}"
                                Content="{materialDesign:PackIcon Menu, Size=24}"/>-->
                        <TextBlock Grid.Column="1"
                                Text="应用设置"
                                Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   HorizontalAlignment="Left"
                                   Margin="0 0 0 0"
                                   VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                            <TextBlock Text="{x:Static ci:App.AppVersion}"
                                       VerticalAlignment="Center"
                                       ToolTip="应用版本"/>

                            <materialDesign:PopupBox Margin="8 0 -8 0"
                                    ToolTip="更多选项…"
                                    StaysOpen="False"
                                                     IsPopupOpen="{Binding ViewModel.IsPopupMenuOpened, Mode=TwoWay}">
                                <StackPanel>
                                    <MenuItem Icon="{materialDesign:PackIcon Code}"
                                            Header="实验性功能…"
                                            Click="MenuItemExperimentalSettings_OnClick"/>
                                    <MenuItem Icon="{materialDesign:PackIcon TextBoxOutline}"
                                            Header="查看日志…"
                                            Click="ButtonViewLogs_OnClick"/>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </Grid>
                </materialDesign:ColorZone>
            </Grid>
        </materialDesign:DrawerHost>
    </materialDesign:DialogHost>
</ely:MyWindow>
